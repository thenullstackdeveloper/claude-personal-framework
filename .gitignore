node_modules/
dist/
coverage/
target/
*.tsbuildinfo
*.log
.DS_Store

# Claude Code local config (no secrets / no machine-specific state)
.claude/settings.local.json
.claude/.cache/

# Output of `claude-fw install` — sources of truth live in agents/, skills/, commands/
.claude/agents/
.claude/skills/
.claude/commands/

# Output of `claude-fw install` — sources of truth live in base.yaml git-hooks:
# entries + the catalog. Fresh-clone friction tracked as Deferred 16.
.githooks/

# Token de Plane MCP (no commitear)
.mcp.json
